{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RideAssigned",
  "type": "object",
  "required": ["eventId", "type", "rideId", "driverId", "assignedAt"],
  "properties": {
    "eventId": { "type": "string" },
    "type": { "const": "RideAssigned" },
    "rideId": { "type": "string" },
    "driverId": { "type": "string" },
    "assignedAt": { "type": "string" }
  }
}
