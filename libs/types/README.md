# libs/types
Shared infra-level utilities (no business logic sharing).

## OpenAPI generated types

Regenerate types when OpenAPI specs change:

```bash
npm run generate:review-service --workspace @libs/types
npm run generate:ride-service --workspace @libs/types
```
